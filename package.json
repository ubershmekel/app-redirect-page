{
  "name": "app-redirect-page",
  "version": "1.0.0",
  "description": "A snippet to turn a page into an app redirect page",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "build:watch": "tsdown --sourcemap --watch",
    "serve": "http-server . -p 4173 -c-1",
    "dev": "concurrently \"npm:build:watch\" \"npm:serve\""
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "http-server": "^14.1.1",
    "tsdown": "^0.18.3"
  }
}
